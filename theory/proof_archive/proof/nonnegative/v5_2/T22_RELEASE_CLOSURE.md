# T22 certificate release closure

## Resolved review findings

1. Corrected `31 files` to `32 canonical proof files`.
2. Added every deterministic `formal_gpp_chunk_*.json` to the release manifest: 23 chunks.
3. Expanded the v5.1 manifest to 59 entries after adding provenance, environment, release notes, and CI workflow.
4. Replaced unqualified `immutable manifest` wording with `fixed verification hashes`; external authentication requires publishing the archive digest outside the archive.
5. Pinned the Python dependency: `mpmath==1.3.0`.
6. Added a GitHub Actions matrix for Ubuntu/macOS and Python 3.11–3.13.
7. Added explicit archive provenance and trust-base documentation.
8. Quarantined the exact v4 archive and theorem-JSON hashes that carried the stale two-sided interpretation.

## Current verification

`verify_theorem_package.py` passes and reports:

- 1,421 formal pointwise subintervals;
- global pointwise margin `-1.0045862406584556e-13`;
- directed kernel-mean interval width `2.288700332700929e-22`;
- one-sided logic verified.

`verify_manifest.py` passes:

`manifest verified: 59 files`.

## Honest limit of this current run

This coordinator pass regenerated selected chunks and verified the shipped deterministic artifacts and expanded manifest. It did not complete a fresh serial regeneration of all 1,421 pointwise boxes within the current command budget. Agent 2's earlier clean-room audit is the source for the claim that the entire original computation regenerated byte-for-byte. The v5.1 wrapper makes that process more auditable but should still be rerun in CI before publication.

## External release requirement

After creating the final v5.1 archive, publish its SHA-256 in an external immutable location (release page, archival DOI record, signed commit, or equivalent). An archive cannot independently authenticate its own digest.
