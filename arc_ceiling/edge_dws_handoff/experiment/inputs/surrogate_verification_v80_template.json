{
  "verified": true,
  "proof_kind": "additive_final_output",
  "statement": "The frozen V80 correction_direction is already a final-output vector and candidate(scale) = baseline - scale * correction_direction, so replay error is affine in the scalar coefficient exactly.",
  "upstream_code_sha256": "REPLACE_WITH_FROZEN_EXPORTER_HASH"
}
