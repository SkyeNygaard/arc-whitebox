# Prompt 7 — One width-256 edge-state DWS predictor

Train one actual-width permutation-equivariant edge-state Deep Weight Space model only after a frozen low-dimensional Prompt 4–6 label exists. Group every rotation of a base network in one fold; optimize scored final replay or an exactly verified linear surrogate; compare with anchor-only, constant shrinkage, and invariant ridge controls; report leakage/equivariance, grouped replay metrics, calibration, tails, hashes, and inference cost.

**Preregistered gate:** grouped raw replay gain ≥1.15; adjusted 95% interval strictly above 1; worst base-network candidate/baseline ≤1.10; inference cost repaid.

**Terminal state for this package:** `EXTERNALLY_BLOCKED`. The current Library snapshot does not contain a frozen Prompt 4–6 label/corpus bundle or the canonical split registry, and the current runtime has no CUDA device. No validation or test split was opened and no scientific model-class failure is claimed.
