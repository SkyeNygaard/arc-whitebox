# Strongest defensible claims after the Agent 2 strengthening round

## Claim A — numerical theorem

**Status: COMPUTER-ASSISTED CERTIFIED.**

For the explicit dimension-256, depth-32 infinite-width normalized ReLU kernel, the 66,048-point antipodal real-MUB rule has ensemble kernel discrepancy at most `1.0002336550102949` times the infimum over network-independent nonnegative-weight probability cubature rules with at most 66,048 support points. Equivalently, its true relative excess is in `[0, 0.02336550102949%]`.

The statement is one-sided and does not prove strict suboptimality.

## Claim B — existence and incidence of the evaluated point set

**Status: COMPUTER-ASSISTED CERTIFIED by explicit finite construction.**

The strengthening certificate constructs 128 signed-Walsh bases plus the coordinate basis in dimension 256, verifies every one of the 8,128 nontrivial basis pairs has normalized cross-inner-product magnitude `1/16`, and derives the fixed-node multiplicities `1,1,510,32768,32768`. The reconstructed 128-by-256 chirp array exactly equals the archived production asset.

This means the numerical theorem no longer needs to trust an uninspected external claim that a suitable 129-basis family exists. The classical Kerdock literature remains the mathematical provenance for the construction, but the incidence property actually used by the theorem is checked directly.

## Claim C — arithmetic implementation diversity

**Status: PARTIALLY INDEPENDENTLY REPRODUCED.**

A pure-Python `_pydecimal` path regenerated the assembled certificate, sign audit, multiplicity proof, and spherical kernel-mean certificate byte-for-byte; it also regenerated three representative curvature chunks byte-for-byte and independently recomputed the final scalar theorem. It did not regenerate the entire curvature mesh.

## Claim D — release reproducibility

**Status: RELEASE-HARDENED.**

The full release manifest tracks all proof source, all 23 curvature chunks, all final certificates, and all strengthening artifacts. The release archive has a separately distributed SHA-256 digest.

This is stronger than the original 32-entry manifest, but it is still not a digital signature, external timestamp, or proof-assistant artifact.
