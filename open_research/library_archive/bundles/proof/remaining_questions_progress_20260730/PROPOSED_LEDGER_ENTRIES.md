# Proposed namespaced ledger entries

## WHB-T-SIGNED-BLOCKTRACE-SHARPNESS-20260730

**Status:** proved, classical Schur-Horn input.

**Claim:** the rank plus individual harmonic block-trace relaxation used by the signed certificate is exactly sharp over abstract symmetric matrices for every released profile. No stronger bound can follow from those constraints alone.

**Scope:** abstract moment matrices; does not assert point-evaluation realizability.

## WHB-T-SIGNED-EQUALITY-ZEROCODE-20260730

**Status:** proved under full active-node rank.

**Claim:** an actual signed atomic rule attains a profile's abstract rank floor only if it has exactly `N` equal positive weights and pairwise zero feature-kernel inner products.

**Use:** converts the residual rank gap into a spherical zero-code/realizability problem.

## WHB-D-CONTINUOUS-CONE-OVERFLOW-20260730

**Status:** numerical audit.

**Claim:** the original continuous-radius stationary-point search overflowed and missed interior minima. After rescaling repair, the continuous adjacent numerical optimum is `0.9370496015`, only `0.00036446` percentage point above the exact released witness.

## WHB-T-FW-SUBCERT-FRONTIER-20260730

**Status:** exact corollary of released signed certificate.

**Claim:** truncating the witness yields exact coefficient-degree frontiers; 90% needs degrees through 128 and 93% through 194.

## WHB-E-SUPPORT-CAPACITY-MENU-20260730

**Status:** oracle diagnostic on archived high-reference development data.

**Claim:** at `K=32`, target-labeled best-of-four support selection captures `40.53%` on rotation 19 and `48.54%` pooled, below the 50% gate. Amplitude-learning descendants on the exact support menu are closed.

## WHB-D-ORACLE-CONSTANT-FIRST-20260730

**Status:** diagnostic.

**Claim:** the feature policy frequently reverses the globally useful first source coefficient and is worse than the global rule on confirmation. Future models must predict residual coefficients around the frozen global action.
