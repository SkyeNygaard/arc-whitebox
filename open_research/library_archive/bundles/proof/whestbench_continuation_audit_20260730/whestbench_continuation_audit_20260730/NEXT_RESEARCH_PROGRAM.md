# Next research program

## Priority 1 — canonical v5.2 theorem release

Merge the completed T16 optimum into T22:

1. create one canonical one-sided theorem JSON;
2. update the certified lower interval and ratio to the `0.023324172950039%` bound;
3. extend the C/GMP theorem assembler to consume the T16 optimum certificate;
4. add a verifier that rejects the old bound when the T16 certificate is declared present;
5. externally anchor the final archive digest.

## Priority 2 — independent T16 primal engine

Reimplement, without importing the Python proof code:

- contact-root enclosures;
- `F''/F'` recurrence;
- `kappa^(6)+3B_(6,2)` interval proof;
- Krawczyk coefficient enclosure;
- Hermite remainder feasibility;
- primal-dual equality and uniqueness.

Preferred stack: Arb or MPFI; alternative: Lean/Coq/Isabelle for analytic skeleton plus certified numerics.

## Priority 3 — arbitrary-node finite-width T22

The exact finite-width T27 theorem solves only the fixed line universe. The remaining theorem needs a finite-width Delsarte lower certificate or a structure-aware finite/infinite kernel comparison far sharper than a uniform sup-norm bound.

Start with:

1. exact or certified finite-width association and low-degree harmonic coefficients;
2. numerical search for a finite-width minorant at widths 64, 128, 256, 512;
3. test whether T16 contact topology persists;
4. derive width-dependent coefficient inequalities from the Hermite/noise-stability representation;
5. freeze any conjectured finite-width certificate before large exact computation.

## Priority 4 — actual observation symmetry

Turn the group theorem into a real WHestBench theorem or a constructive counterexample:

1. specify the legal runtime observation map exactly;
2. identify candidate network/rotation transformations;
3. prove whether the observation is invariant;
4. calculate how the desired signed correction transforms;
5. if exact invariance fails, measure the symmetry-breaking component and build the optimal low-dimensional predictor from it;
6. use grouped frozen validation and matched constants.

## Priority 5 — release repair for empirical negatives

- vendor `arc_experiments.py`, `agent9_10_oracle_bundle`, and exact Kerdock assets into ReopenedPaths;
- include fresh-anchor vector NPZs and upstream T4 code in Agent5;
- provide one clean command that regenerates every summary from source rows;
- retain protected cohort declarations and split hashes.
