# Reproduction

Run `bash run_core_checks.sh` from this directory. It recomputes the strengthened degree-23 certificate with both independent stacks and compares headline outputs.

The within-network and orientation-odd analyses depend on large authenticated upstream archives. Their scripts and frozen outputs are included, but the large source arrays are referenced rather than duplicated.
