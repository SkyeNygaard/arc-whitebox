# WHestBench latest proof-frontier review — v21

**Date:** 2026-07-30 14:33 ET

## Canonical changes since v20

1. The strongest static arbitrary-signed limiting-kernel theorem is now the degree-280 block-trace certificate:
   `R(Q) >= 0.9370459569114724 R_K`.
   The same-cost raw-improvement cap is `1.067183517x`. At the recorded `4.34x` adjusted gap, even a floor-attaining rule needs evaluation cost at most `0.24589482` of baseline.

2. The rank plus individual harmonic block-trace relaxation is exactly sharp over abstract symmetric matrices. It cannot be strengthened by more optimization inside that relaxation.

3. Equality for a real atomic point-evaluation rule is rigid: exactly `N` equal positive weights and pairwise zero comparison-kernel inner products. The remaining static theorem gap is a spherical zero-code / shared-moment realizability problem.

4. The finite-width proof burden is compressible. A valid 90% subcertificate needs finite-width coefficient control only through degree 128; 93% needs degree 194.

5. The prior continuous-radius saturation claim contained an overflow bug. The corrected continuous and multiblock numerical frontiers improve the exact theorem by less than 0.001 percentage point. Further radius-grid engineering is not a promising path.

6. The within-network Oracle repair interface is genuinely multi-component: confirmation effective rank is about 4.11 and every base needs four or five components for 90% energy.

7. The tested K<=32 support menu fails before coefficient learning: even target-labeled best-of-four support selection captures only 48.54% pooled and 40.53% on rotation 19.

8. The five-source coefficient model should be reframed constant-first. It frequently reverses the strongest globally useful coefficient; future policies must predict only a bounded residual around the frozen global action.

## Highest-value paths

1. Joint spherical moment realizability / zero-code theorem.
2. Width-256 degree-128 finite-width transfer.
3. Four-channel multi-checkpoint contraction identity.
4. Constant-first orientation-aware residual policy.
5. Exact nonlinear ReLU replay certificate.
6. Exactly integrable residual-spectrum design.
7. Realistic computational white-box lower bound.
8. Hostile cross-agent scope and score audit.

## Operational conclusion

No deployable estimator has emerged. Static signed equal-cost cubature is now essentially closed as a winning route. Breakthrough work should target architecture-specific adaptive identities, actual-width harmonic certificates, or residual transformations with a theorem-level final-output and score argument.
